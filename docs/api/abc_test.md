# ABC Test

```{eval-rst}
.. autoclass:: ab.api.endpoints.abc_test.ABCTestEndpoint
   :members:
   :undoc-members:
```

## Methods

Diagnostic endpoints on the ABC API surface.

### get_contact

`GET /Test/contact` — Get test contact (optionally by crmContactId).

### get_recent_estimates

`GET /Test/recentestimates` — Get recent estimates.

### get_rendered_template

`GET /Test/renderedtemplate` — Get rendered template.
